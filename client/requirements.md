## Packages
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Using HTML5 Canvas API for client-side image generation.
No external image processing libraries required to keep the bundle small.
Fonts: 'Outfit' for UI, 'Anton' for meme text (classic impact-style).
